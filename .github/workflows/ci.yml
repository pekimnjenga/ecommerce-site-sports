name: Code Quality and Security Checkst

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-test-security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8 black safety isort 

      - name: Run black check
        run: black --check .

      - name: Run isort check
        run: isort . --check-only

      - name: Run flake8
        run: flake8 .

      - name: Run safety
        run: safety check --file=requirements.txt --full-report --output json

